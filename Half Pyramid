#include<iostream>
using namespace std;
int main()
{
    int n;
    cout << "Enter the numbers of rows: ";
    cin >> n;

    for (int k = 1; k <= n; k++)
    {
        for (int l = 1; l <= k; l++)
        {
            cout << "* ";
        }
        cout << endl;
    }
    return 0;
}
